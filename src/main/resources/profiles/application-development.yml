# SQL Database Information
spring:
  datasource:
    username: admin
    password: admin
    url: jdbc:mysql://localhost:3306/taskmanagement?useSSL=false
server: 8080

# Log SQL
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE